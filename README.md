# 数控刀具寿命闭环

本项目统一记录数控刀具从入库、装刀、加工、修磨到报废的生命周期。刀具身份与机床刀位分离，任何调拨都不重置累计加工证据。

`domain_contract.json` 定义刀具状态、寿命计量方式和开工授权结论。件数、切削时长与异常冲击可同时存在，规则版本按零件、材料和生效区间选择。

运行基础检查：

```bash
python -m unittest discover -s tests -v
```

